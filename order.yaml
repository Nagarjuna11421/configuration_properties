text:
    message: Good Nagarjuna, thiis config server approach
    
management:
  endpoints:
    web:
      exposure:
        include: *
management:
  endpoints:
    web:
      exposure:
        exclude: refresh
